function printSurfaceArea(x, y){
    var width = x;
    var height = y;
    var area = width * height;
    console.log("Surface area of: ("+x+" x "+y+") is: "+area);
}


var width = 50;
var height = 60;

printSurfaceArea(width, height)